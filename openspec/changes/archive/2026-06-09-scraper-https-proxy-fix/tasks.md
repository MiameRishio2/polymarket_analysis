## 1. Regression Test

- [x] 1.1 Add a failing test that scraper proxy configuration is used for HTTPS requests.

## 2. Fix

- [x] 2.1 Change scraper proxy construction to all-scheme proxying.

## 3. Verification

- [x] 3.1 Run focused proxy regression test.
- [x] 3.2 Run `cargo test --all`, `cargo build`, and `node tests/menu_page_config_test.js`.
- [x] 3.3 Update `session.md`, `change_log.md`, and `test.md`.
