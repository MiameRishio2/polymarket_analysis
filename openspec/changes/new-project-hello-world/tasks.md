# Tasks: new-project-hello-world

## 实现任务

- [x] 创建 `Cargo.toml`，定义 axum、serde、tokio 依赖
- [x] 创建 `src/lib.rs`，导出 handlers 模块
- [x] 创建 `src/handlers.rs`，实现 GET /api/hello Handler
- [x] 创建 `src/main.rs`，构建 Axum Router 并启动服务
- [x] 创建 `public/index.html`，Hello World 页面
- [x] 验证构建：`cargo build` 成功
- [x] 验证运行：服务启动，页面可访问，API 返回正确

## 架构规范文件（补充）

- [x] 创建 `agent.md`，定义 agent 启动任务前的加载规范
- [x] 创建 `architect.md`，文档化前后端/数据层分离架构
- [x] 创建 `session.md`，记录任务状态与变更日志
